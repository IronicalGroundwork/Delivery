<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Delivery" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/moderate_aquamarine</item>
        <item name="colorPrimaryVariant">@color/moderate_aquamarine</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.Delivery.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Delivery.SplashTheme" parent="Theme.Delivery.NoActionBar">
        <item name="android:background">@drawable/splash</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/black_blue</item>
    </style>

    <style name="Theme.Delivery.LoginTheme" parent="Theme.Delivery.NoActionBar">
        <item name="android:windowLightStatusBar" tools:targetApi="23">true</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/white</item>
    </style>

    <style name="Theme.Delivery.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Delivery.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ThemeOverlay.AppCompat.navTheme">
        <item name="colorControlHighlight">@color/transparent</item>
        <item name="itemTextAppearance">?attr/textAppearanceLargePopupMenu</item>
        <item name="itemIconTint">@color/black_blue</item>
        <item name="itemTextColor">@color/black_blue</item>
    </style>
</resources>